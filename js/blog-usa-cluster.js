/* USA notes — do not overwrite core published posts with thin stubs.
   Core articles (benefits, bilona vs regular, price, purity) live in data.js / scheduled files. */
(function () {
  // Intentionally empty: earlier versions replaced live posts with 2-line USA CTAs.
  // Country-specific USA articles live in blog-month1-sprint.js.
})();
